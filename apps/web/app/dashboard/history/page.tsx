import ThreadsShowcase from "@/components/dashboard/history/history";
import React from "react";

type Props = {};

const historyPage = (props: Props) => {
  return (
    <div>
      <ThreadsShowcase />
    </div>
  );
};

export default historyPage;
